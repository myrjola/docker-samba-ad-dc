#!/usr/bin/expect

spawn /usr/sbin/kdb5_util create -s
expect "Enter KDC database master key: "
send -- "relexkerberos\r"
expect "Re-enter KDC database master key to verify: "
send -- "relexkerberos\r"
expect "\r"
send -- "exit\r"
expect eof
